# The agent runtime's LLM adapters are httpx-based (no provider SDKs). sqlalchemy comes from the postgres
# tool, arq/redis from the redis tool — both declared as dependencies; merged into the Core image at build.
httpx==0.28.1
